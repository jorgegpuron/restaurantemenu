# Ni una conversión de fin de línea, en ninguna dirección. Git en Windows viene con
# core.autocrlf activado y al hacer checkout dejaría gen.mjs en CRLF; como el HTML se emite
# desde sus plantillas, la carta cambiaría de bytes sin que nadie haya tocado una línea.
# Todo el proyecto es LF hoy y así se queda. Se guarda lo que hay y se devuelve lo que hay.
* -text
